// const redisClient = require('../utils/redis');

// (async () => {
//   console.log(redisClient.isAlive());
//   // console.log(await redisClient.get('myKey'));
//   await redisClient.set('alaa', 12, 500);
//   // console.log(await redisClient.get('hoda'));

//   setTimeout(async () => {
//     console.log(await redisClient.get('myKey'));
//   }, 1000 * 10);
// })();
